# Shared optional vars (copy to .env — gitignored). Supabase credentials use profiles:
#   Local:  cp .env.local.example .env.local
#   Cloud:  cp .env.cloud.example .env.cloud
# Mirror active SUPABASE_* into .dev.vars for Cloudflare workerd (npm run dev / dev:local / dev:cloud).

# Optional — generation (F-01). Omit for mock provider in dev/CI.
# AI_PROVIDER=mock
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash-lite
